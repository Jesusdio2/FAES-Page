bots:
  - name: faes-bot
    user_agent_regex: ".*curl.*"
    action: block
    decay: 0.5
    max_requests: 100
    window: 60s

decaymap:
  - path: /api/anubis
    allow: true
  - path: /
    allow: true
