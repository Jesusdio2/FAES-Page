decaymap:
  - path: /api/anubis
    allow: true
    action: static
    body: "Respuesta directa de Anubis en /api/anubis"

  - path: /anubis-check
    allow: true
    action: static
    body: "Anubis está vivo y respondiendo"

  - path: /
    allow: true
    action: static
    body: "Bienvenido al altar FAES (Anubis)"
